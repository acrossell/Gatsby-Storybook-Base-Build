import React from 'react';
import { storiesOf } from '@storybook/react';

import './DefaultLayout.scss';
import Layout from '../DefaultLayout/DefaultLayout';

storiesOf('layouts/Default Layout', module).add('Default Layout', () => (
  <Layout>
    
  </Layout>
));
